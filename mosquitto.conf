# Config básica

# Los puertos se configurarán dinámicamente por el script de inicio
listener ${PORT}
protocol mqtt

# Permitir conexiones desde cualquier IP
allow_anonymous true

# Configuración de rendimiento
max_connections -1
max_queued_messages 1000